version: '3.8'

services:
  app:
    build: .
    container_name: secure_file_manager
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/app/storage
    env_file:
      - .env
    restart: unless-stopped